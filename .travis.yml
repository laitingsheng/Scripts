dist: xenial
language: python
sudo: required
services:
  - docker

jobs:
  include:
    - script:
        - sudo docker run -v $PWD:/build ubuntu:bionic /bin/bash -c "cd /build; bash ci/ubuntu.sh $(config)"
      env:
        matrix:
          - config=base
          - config=desktop
