ubuntu:base:
  stage: test
  image: ubuntu:bionic
  script:
    - bash ./ubuntu.base.sh -d bionic -l us -u $(whoami)

ubuntu:desktop:
  stage: test
  image: ubuntu:bionic
  script:
    - bash ./ubuntu.desktop.sh -d bionic -l us -u $(whoami)
